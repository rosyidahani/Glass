<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="shop" name="Toko Koin Mahasiswa">
        <t t-call="custom_web.layout">
            <!-- Fetch active shop items from database -->
            <t t-set="avatar_items" t-value="request.env['shop.avatar'].sudo().search([('active', '=', True)])"/>
            <t t-set="voucher_items" t-value="request.env['shop.voucher'].sudo().search([('active', '=', True)])"/>

            <!-- Inject dynamic shop item metadata for frontend modals -->
            <script type="text/javascript">
                window.avatarData = {
                    <t t-foreach="avatar_items" t-as="item">
                        "<t t-esc="item.avatar_id"/>": { 
                            "name": "<t t-esc="item.nama_avatar"/>", 
                            "price": <t t-esc="item.harga_koin"/>, 
                            "img": "<t t-esc="item.image_src"/>" 
                        },
                    </t>
                };
                window.voucherData = {
                    <t t-foreach="voucher_items" t-as="item">
                        "<t t-esc="item.code"/>": { 
                            "name": "<t t-esc="item.name"/>", 
                            "price": <t t-esc="item.price"/>, 
                            "prefix": "<t t-esc="item.voucher_prefix or 'VOU'"/>" 
                        },
                    </t>
                };
            </script>

            <!-- Hidden coin data from database for initialization -->
            <span id="raw-student-coins" style="display: none;"><t t-esc="mahasiswa.koin"/></span>

            <div class="shop-container">
                <t t-call="custom_web.shared_student_header">
                    <t t-set="title" t-value="'Toko Koin'"/>
                    <t t-set="back_url" t-value="'/dashboard/mahasiswa'"/>
                    <t t-set="show_coins" t-value="True"/>
                </t>

                <!-- Tab Segmented Control -->
                <div class="shop-tabs-wrapper">
                    <div class="shop-tabs">
                        <button type="button" class="shop-tab-btn active" id="tab-avatar-btn" onclick="switchShopTab('avatar')">
                            <i class="bi bi-person-circle"></i> <span data-translate-id="shop_tab_avatar">Avatar</span>
                        </button>
                        <button type="button" class="shop-tab-btn" id="tab-voucher-btn" onclick="switchShopTab('voucher')">
                            <i class="bi bi-gift"></i> <span data-translate-id="shop_tab_voucher">Voucher</span>
                        </button>
                    </div>
                </div>

                <!-- Main Content Panes -->
                <div class="shop-content-section">
                    
                    <!-- PANE 1: AVATAR SHOP -->
                    <div class="shop-pane active" id="shop-avatar-pane">
                        <div class="avatar-grid">
                            <t t-foreach="avatar_items" t-as="item">
                                <div t-attf-class="avatar-card glass-panel #{'equipped-active' if mahasiswa.equipped_avatar_id.id == item.id else ''}" t-att-data-id="item.avatar_id">
                                    <div class="avatar-card-image-box">
                                        <img t-att-src="item.image_src" t-att-alt="item.nama_avatar"/>
                                    </div>
                                    <div class="avatar-info-box">
                                        <h4 class="avatar-name"><t t-esc="item.nama_avatar"/></h4>
                                        <p class="avatar-desc"><span data-translate-id="shop_cool_avatar">Avatar keren</span> <t t-esc="item.nama_avatar"/>.</p>
                                    </div>
                                    
                                    <!-- Action Button based on state -->
                                    <t t-if="mahasiswa.equipped_avatar_id.id == item.id">
                                        <button type="button" class="avatar-action-btn btn-equipped" t-att-data-id="item.avatar_id">
                                            <i class="bi bi-check-circle-fill"></i> <span data-translate-id="shop_btn_equipped">Digunakan</span>
                                        </button>
                                    </t>
                                    <t t-elif="item in mahasiswa.owned_avatar_ids">
                                        <button type="button" class="avatar-action-btn btn-equip" t-att-data-id="item.avatar_id" data-translate-id="shop_btn_equip">
                                            Pasang
                                        </button>
                                    </t>
                                    <t t-else="">
                                        <button type="button" class="avatar-action-btn btn-buy" t-att-data-id="item.avatar_id">
                                            <i class="bi bi-coin"></i> <span data-translate-id="shop_btn_buy">Beli</span> <t t-esc="item.harga_koin"/>
                                        </button>
                                    </t>
                                </div>
                            </t>
                        </div>
                    </div>

                    <!-- PANE 2: VOUCHER SHOP -->
                    <div class="shop-pane" id="shop-voucher-pane">
                        <div class="voucher-list">
                            <t t-set="color_classes" t-value="['bar-green', 'bar-amber', 'bar-blue', 'bar-purple']"/>
                            <t t-set="idx" t-value="0"/>
                            <t t-foreach="voucher_items" t-as="item">
                                <t t-set="color_class" t-value="color_classes[idx % 4]"/>
                                <t t-set="idx" t-value="idx + 1"/>
                                
                                <t t-set="claimed" t-value="request.env['shop.transaction'].sudo().search([('mahasiswa_id', '=', mahasiswa.id), ('item_type', '=', 'voucher'), ('voucher_id', '=', item.id)], limit=1)"/>
                                
                                <div class="voucher-card glass-panel" t-att-data-id="item.code">
                                    <div t-attf-class="voucher-left-bar #{color_class}"></div>
                                    <div class="voucher-main-body">
                                        <h4 class="voucher-title"><t t-esc="item.name"/></h4>
                                        <p class="voucher-desc"><t t-esc="item.description or ''"/></p>
                                        <span class="voucher-price-tag"><i class="bi bi-coin"></i> <t t-esc="item.price"/> <span data-translate-id="dash_koin">Poin</span></span>
                                    </div>
                                    <div class="voucher-divider"></div>
                                    <div class="voucher-right-action">
                                        <t t-if="claimed">
                                            <button type="button" class="voucher-action-btn btn-claimed" t-att-data-id="item.code" disabled="disabled" data-translate-id="shop_btn_claimed">Ditukar</button>
                                        </t>
                                        <t t-else="">
                                            <button type="button" class="voucher-action-btn btn-claim" t-att-data-id="item.code" data-translate-id="shop_btn_claim">Tukar</button>
                                        </t>
                                    </div>
                                </div>
                            </t>
                        </div>
                    </div>


                </div>
            </div>
        </t>
    </template>
</odoo>
