<odoo>
    <template id="dashboard" name="Dashboard Page">
        <t t-call="custom_web.layout">
            <link rel="stylesheet" href="/custom_web/static/src/css/dashboard.css?v=12"/>

            <div class="dashboard-container">
                <!-- TOP HUD -->
                <div class="top-hud">
                    <div class="hud-left">
                        <!-- Profile Card without EXP -->
                        <div class="profile-pill glass-panel">
                            <div class="avatar-large">
                                <t t-if="mahasiswa.foto_profil">
                                    <img t-att-src="'data:image/png;base64,' + mahasiswa.foto_profil.decode('utf-8')" alt="Profile"/>
                                </t>
                                <t t-else="">
                                    <img src="/custom_web/static/src/img/avatar_default.png" alt="Profile"/>
                                </t>
                            </div>
                            <div class="profile-info">
                                 <div class="profile-name"><t t-esc="mahasiswa.name"/></div>
                                 <div class="profile-level"><t t-esc="mahasiswa.nim"/></div>
                             </div>
                             <!-- Rank Badge on the right inside profile-pill -->
                             <div class="profile-rank-badge" title="Rank Leaderboard">
                                 <span>#<t t-esc="rank"/></span>
                             </div>
                        </div>
                        
                        <!-- Action Buttons stacked below profile pill -->
                        <div class="hud-actions-vertical">
                             <!-- Shop Button -->
                            <a href="/dashboard/mahasiswa/shop" class="icon-btn shop-btn glass-panel" data-translate-title-id="title_shop" title="Shop">
                                <i class="bi bi-bag-fill"></i>
                            </a>
                            <!-- Setting Button -->
                            <a href="/dashboard/mahasiswa/settings" class="icon-btn setting-btn glass-panel" data-translate-title-id="title_settings" title="Pengaturan Akun">
                                <i class="bi bi-gear-fill"></i>
                            </a>
                        </div>
                    </div>

                    <!-- Right vertical stack of 2 stats pills -->
                    <div class="hud-right-vertical">
                        <!-- Coin/Poin (Top) -->
                        <div class="coin-pill" data-translate-title-id="title_coins_exchange" title="Koin yang dapat ditukar">
                            <i class="bi bi-coin"></i> <span class="student-coins-amount"><t t-esc="mahasiswa.koin"/></span> <span data-translate-id="dash_koin">Poin</span>
                        </div>

                        <!-- EXP Pill (Bottom) -->
                        <div class="xp-pill" data-translate-title-id="title_total_xp" title="Total XP">
                            <i class="bi bi-star-fill"></i> <t t-esc="mahasiswa.total_xp"/> XP
                        </div>
                    </div>

                </div>

                <!-- MAIN DASHBOARD CONTENT -->
                <div class="main-dashboard-area">
                    
                    <!-- LEFT: 2D Character Area -->
                    <div class="avatar-section glass-panel">
                        <div class="avatar-glow"/>
                        <img t-att-src="mahasiswa.equipped_avatar_id.image_src or '/custom_web/static/src/img/avatar_default_character_l.png'" class="user-image" alt="Character Glass"/>
                    </div>

                </div>

            </div>
        </t>
    </template>
</odoo>
